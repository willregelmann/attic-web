{
  "name": "attic-docs-screenshots",
  "version": "1.0.0",
  "description": "Playwright-based screenshot generator for Will's Attic user guides",
  "scripts": {
    "generate": "playwright test",
    "generate:ui": "playwright test --ui",
    "generate:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
